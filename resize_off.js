            var win_width=800;
            var win_height=800;
            var win_x_pos=50;
            var win_y_pos=50;
            $(document).ready(function(){window.resizeTo(win_width,win_height)});
            $(window).resize(function()
            {
            window.resizeTo(win_width,win_height)
            });